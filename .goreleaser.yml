# GoReleaser configuration for gopowerwall
# Documentation: https://goreleaser.com

version: 2

before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - id: gopowerwall
    main: ./cmd/gopowerwall
    binary: gopowerwall
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    tags: [netgo, osusergo, static_build]
    ldflags:
      - -s -w
      - -X github.com/blackbirdworks/gopowerwall/pkgs/version.BuildVersion={{.Version}}
      - -extldflags "-static"
    ignore:
      - goos: windows
        goarch: arm64

  - id: proxy
    main: ./cmd/proxy
    binary: proxy
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    tags: [netgo, osusergo, static_build]
    ldflags:
      - -s -w
      - -X github.com/blackbirdworks/gopowerwall/pkgs/version.BuildVersion={{.Version}}
      - -extldflags "-static"
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: gopowerwall-archive
    ids: [gopowerwall, proxy]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - 'merge conflict'
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
  groups:
    - title: 'New Features'
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: 'Bug Fixes'
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: 'Performance Improvements'
      regexp: "^.*perf[(\\w)]*:+.*$"
      order: 2
    - title: 'Refactors'
      regexp: "^.*refactor[(\\w)]*:+.*$"
      order: 3
    - title: 'Other Changes'
      order: 999

dockers_v2:
  - id: proxy
    dockerfile: Dockerfile.goreleaser
    ids: [proxy]
    images:
      - "ghcr.io/blackbirdworks/gopowerwall"
    tags:
      - "{{ .Version }}"
      - "{{ if not .IsSnapshot }}latest{{ end }}"
    labels:
      org.opencontainers.image.created: "{{.Date}}"
      org.opencontainers.image.title: "{{.ProjectName}}"
      org.opencontainers.image.revision: "{{.FullCommit}}"
      org.opencontainers.image.version: "{{.Version}}"
      org.opencontainers.image.source: "{{.GitURL}}"
    platforms:
      - linux/amd64
      - linux/arm64

release:
  github:
    owner: blackbirdworks
    name: gopowerwall
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## gopowerwall {{ .Tag }}

    **Full Changelog**: https://github.com/blackbirdworks/gopowerwall/compare/{{ .PreviousTag }}...{{ .Tag }}
  footer: |
    ## Docker

    ```bash
    docker pull ghcr.io/blackbirdworks/gopowerwall:{{ .Version }}
    ```

    ## Installation

    ### Binary
    Download the appropriate archive for your platform from the assets below.

    ### From source
    ```bash
    go install github.com/blackbirdworks/gopowerwall/cmd/gopowerwall@{{ .Tag }}
    ```
